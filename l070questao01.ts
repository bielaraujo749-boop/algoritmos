let nomes: string[]= ["Ana", "Bruna", "Carla", "Daniela", "Elaine"];
let profs: string[]= ["dentista", "contadora", "médica", "enfermeira", "desempregada"];

for (let i: number = 0; i < nomes.length; i++) {
    console.log(`${nomes[i]} é ${profs[i]}`)
} 